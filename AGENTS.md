# Lux Hotel Agent Instructions

This project is part of the xomnhala.vn platform. Follow `/Users/vivu/Desktop/AGENTS.md`.

## App

- Domain: `hotel.xomnhala.vn`
- Local URL: `http://127.0.0.1:7200`
- Static server: `server.py`
- Frontend files: `Lux Hotel 1/index.html`, `Lux Hotel 1/style.css`, `Lux Hotel 1/dom.js`
- Assets: `Lux Hotel 1/Images`

## Rules

- Preserve the `.NET` backend solution under `src/` and `tests/` unless the user asks for backend work.
- Keep image paths case-sensitive and rooted in `Lux Hotel 1/Images`.
- Keep frontend deployable as static HTML/CSS/JS.
- Verify JS with `node --check "Lux Hotel 1/dom.js"`.
- Verify server with `python3 -m py_compile server.py`.
